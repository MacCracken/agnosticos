# vllm — High-throughput LLM serving engine
#
# BLFS / AI Stack

[package]
name = "vllm"
version = "0.8.3"
description = "vllm — High-throughput LLM serving engine with PagedAttention"
license = "Apache-2.0"
groups = ["ai", "ml", "llm", "serving"]
release = 1
arch = "x86_64"

[source]
url = "https://github.com/vllm-project/vllm/archive/refs/tags/v0.8.3.tar.gz"
sha256 = "99078eb047a302bcf553aa3c84230631cd4b1288927c326a8be9c714b08faffb"
patches = []

[depends]
runtime = ["python3", "python-pytorch", "python-transformers"]
build = ["python3", "pip"]

[build]
pre_build = ""
configure = ""
make = ""
check = ""
install = "pip install --prefix=/usr --root=$PKG --no-deps ."
post_install = ""

[security]
hardening = ["pie", "fullrelro", "fortify", "stackprotector", "bindnow"]
cflags = "-O2 -pipe"
ldflags = "-Wl,--as-needed"
