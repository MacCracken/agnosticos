[package]
name = "glslang"
version = "15.2.0"
description = "Khronos GLSL/HLSL to SPIR-V shader compiler"
license = "Apache-2.0"
groups = ["desktop"]

[source]
url = "https://github.com/KhronosGroup/glslang/archive/refs/tags/15.2.0.tar.gz"
sha256 = "45e3920d264d5c2cc3bfaec0e5dbb66cffd549255e0aaaf38cd283918e35c8ba"
patches = []

[depends]
runtime = []
build = ["cmake", "ninja", "python"]
optional = []

[build]
pre_build = ""
configure = """
cmake -B build -G Ninja \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DCMAKE_BUILD_TYPE=Release \
    -DBUILD_SHARED_LIBS=ON \
    -DENABLE_OPT=OFF
"""
make = "ninja -C build -j$(nproc)"
check = ""
install = "DESTDIR=$PKG ninja -C build install"

[security]
hardening = ["pie", "fullrelro", "fortify", "stackprotector", "bindnow"]
cflags = "-O2 -pipe"
ldflags = "-Wl,--as-needed"
