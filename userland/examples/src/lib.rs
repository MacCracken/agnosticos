//! AGNOS Examples — shared support code
//!
//! Contains the legacy Agent SDK (originally in `agnos-sys::agent`) for use
//! by example agents. New agent code should use agnostik types directly.

pub mod agent;
